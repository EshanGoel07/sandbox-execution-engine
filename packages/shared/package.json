{
  "name": "@vj/shared",
  "version": "2.0.0",
  "private": true,
  "description": "Cross-cutting TypeScript types shared by the API, worker, engine and web client. Types only — zero runtime code.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  }
}
