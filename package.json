{
  "name": "virtual-judge",
  "private": true,
  "version": "2.0.0",
  "description": "Sandboxed online judge — a code execution engine with a judge as one client",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "lint": "node scripts/check-boundaries.mjs",
    "typecheck": "tsc -b && npm run typecheck -w @vj/web",
    "migrate": "npm run migrate -w @vj/infra",
    "seed": "npm run seed -w @vj/infra",
    "api": "npm run build && node apps/api/dist/api-server.js",
    "worker": "npm run build && node apps/worker/dist/main.js",
    "web": "npm run dev -w @vj/web",
    "test:engine": "npm run build && npm run test -w @vj/engine",
    "test:worker": "npm run build && npm run test -w @vj/worker",
    "test:integration": "npm run build && JWT_SECRET=integration-test-secret TS_NODE_PROJECT=tests/tsconfig.json ts-node tests/integration/submission-lifecycle.ts",
    "test": "npm run lint && npm run test:engine && npm run test:worker && npm run test:integration"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
